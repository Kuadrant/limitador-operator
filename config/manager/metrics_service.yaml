---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: limitador-operator
  name: metrics
  namespace: system
spec:
  ports:
  - name: metrics
    port: 8080
    targetPort: metrics
  selector:
    app: limitador-operator
 
